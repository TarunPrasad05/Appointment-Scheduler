{
    "builds": [{
        "src": "scheduler/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "55mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "scheduler/wsgi.py"
        }
    ]
}